# Environment Variables Example
# Copy this file to .env and update with your actual values

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/video_downloader

# Application Configuration
NODE_ENV=production
PORT=5000

# Optional: Redis for session storage (if using Redis instead of memory store)
# REDIS_URL=redis://localhost:6379

# Optional: JWT Secret for authentication (if implementing user auth)
# JWT_SECRET=your-super-secret-jwt-key

# Optional: API Keys for actual video downloading services
# YOUTUBE_API_KEY=your-youtube-api-key
# INSTAGRAM_API_KEY=your-instagram-api-key